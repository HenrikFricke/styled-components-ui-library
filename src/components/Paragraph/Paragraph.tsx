import styled from 'styled-components';

import {
    colors,
    fonts
} from '../../constants';

export default styled.p`
    color: ${colors.BLACK};
    padding: 5px;
    font-size: ${fonts.STANDARD};
`;